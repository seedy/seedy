import styled from '@mui/material/styles/styled';

import Box from '@mui/material/Box';

// COMPONENTS
const BoxToolbarOffset = styled(Box)(({ theme }) => theme.mixins.toolbar);

export default BoxToolbarOffset;
