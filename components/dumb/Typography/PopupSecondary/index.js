import styled from '@mui/material/styles/styled';

import Typography from '@mui/material/Typography';

// COMPONENTS
const TypographyPopupSecondary = styled(Typography)({
  color: 'rgba(0, 0, 0, 0.54)',
});

export default TypographyPopupSecondary;
